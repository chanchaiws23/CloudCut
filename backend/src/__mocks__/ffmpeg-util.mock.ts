export async function fetchFile(_input: string | URL): Promise<Uint8Array> {
  return new Uint8Array();
}

export async function toBlobURL(_url: string, _mimeType: string): Promise<string> {
  return _url;
}
